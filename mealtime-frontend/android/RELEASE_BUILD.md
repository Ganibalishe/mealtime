# Инструкция по сборке Release версии для Google Play Store

## 📋 Что нужно для публикации в Google Play:

1. ✅ **Keystore** - для подписи приложения
2. ✅ **Иконка приложения** - все размеры
3. ✅ **Release APK/AAB** - подписанная версия

---

## 🔐 Шаг 1: Создание Keystore

### Автоматически (рекомендуется):

```bash
cd android
./generate-keystore.sh
```

Скрипт создаст keystore и попросит ввести пароли. **ВАЖНО: Сохраните пароли в безопасном месте!** Без них вы не сможете обновлять приложение.

### Вручную:

```bash
keytool -genkeypair -v -storetype PKCS12 \
  -keystore app/mealtime-release-key.jks \
  -alias mealtime-key \
  -keyalg RSA -keysize 2048 -validity 9125 \
  -storepass YOUR_STORE_PASSWORD \
  -keypass YOUR_KEY_PASSWORD \
  -dname "CN=Mealtime Planner, OU=Development, O=Mealtime, L=City, ST=State, C=RU"
```

### Создание keystore.properties:

После создания keystore, создайте файл `keystore.properties`:

```properties
storeFile=app/mealtime-release-key.jks
storePassword=YOUR_STORE_PASSWORD
keyAlias=mealtime-key
keyPassword=YOUR_KEY_PASSWORD
```

⚠️ **НЕ КОММИТЬТЕ** `keystore.properties` в git! Он уже добавлен в `.gitignore`.

---

## 🎨 Шаг 2: Создание иконки приложения

### Вариант 1: Использовать онлайн генератор (рекомендуется)

1. Перейдите на https://www.appicon.co/ или https://icon.kitchen/
2. Загрузите вашу иконку (1024x1024px)
3. Скачайте все размеры для Android
4. Замените файлы в папках:
   - `app/src/main/res/mipmap-mdpi/`
   - `app/src/main/res/mipmap-hdpi/`
   - `app/src/main/res/mipmap-xhdpi/`
   - `app/src/main/res/mipmap-xxhdpi/`
   - `app/src/main/res/mipmap-xxxhdpi/`

Нужны файлы:
- `ic_launcher.png` (квадратная иконка)
- `ic_launcher_round.png` (круглая иконка)
- `ic_launcher_foreground.png` (для adaptive icons)

### Вариант 2: Использовать Python скрипт (рекомендуется)

Скрипт берет вашу исходную иконку и создает все нужные размеры:

```bash
# 1. Поместите вашу иконку в папку android/ с именем:
#    - icon.png (рекомендуется)
#    - или icon.jpg, app-icon.png, app-icon.jpg
#    Рекомендуемый размер: 1024x1024px

# 2. Установите Pillow (если еще не установлен)
pip install Pillow

# 3. Запустите скрипт
cd android
python3 generate-icon.py
```

Скрипт:
- ✅ Сохраняет вашу иконку как есть (не меняет содержимое)
- ✅ Масштабирует до всех нужных размеров
- ✅ Создает круглые версии
- ✅ Создает foreground иконки для adaptive icons

### Вариант 3: Создать вручную

Создайте иконку 1024x1024px и используйте инструмент для генерации всех размеров.

**Рекомендуемые размеры:**
- mdpi: 48x48
- hdpi: 72x72
- xhdpi: 96x96
- xxhdpi: 144x144
- xxxhdpi: 192x192

---

## 🔨 Шаг 3: Сборка Release версии

### AAB vs APK - Что выбрать?

**AAB (Android App Bundle)** - рекомендуется для Google Play:
- ✅ Требуется для новых приложений в Google Play (с августа 2021)
- ✅ Меньший размер загрузки для пользователей
- ✅ Google Play оптимизирует APK для каждого устройства
- ❌ Нельзя установить напрямую на устройство

**APK** - для прямой установки:
- ✅ Можно установить напрямую на устройство
- ✅ Подходит для тестирования
- ✅ Можно использовать в альтернативных магазинах
- ❌ Больший размер файла
- ⚠️ Google Play принимает APK только для существующих приложений

### Автоматически (рекомендуется):

```bash
cd android
./build-release.sh aab   # Для AAB (Google Play)
./build-release.sh apk    # Для APK (прямая установка)
./build-release.sh both   # Оба формата
```

### Вручную:

```bash
# 1. Соберите веб-версию
cd mealtime-frontend
npm run build

# 2. Синхронизируйте с Capacitor
npx cap sync android

# 3. Соберите AAB (рекомендуется)
cd android
./gradlew bundleRelease

# Или APK
./gradlew assembleRelease
```

### Результат:

- **AAB**: `app/build/outputs/bundle/release/app-release.aab`
- **APK**: `app/build/outputs/apk/release/app-release.apk`

---

## 📤 Шаг 4: Загрузка в Google Play Console

1. Перейдите в [Google Play Console](https://play.google.com/console)
2. Создайте новое приложение
3. Заполните информацию о приложении:
   - Название: "Mealtime Planner"
   - Краткое описание
   - Полное описание
   - Скриншоты
   - Иконка (512x512px)
   - Графические ресурсы
4. Загрузите AAB файл в разделе "Production" → "Create new release"
5. Заполните "Release notes"
6. Отправьте на проверку

---

## 📝 Дополнительная информация

### Версия приложения

Версия задается в `app/build.gradle`:
- `versionCode` - целое число (увеличивайте при каждом обновлении)
- `versionName` - строка версии (например, "1.0.0")

### Минимальные требования для Google Play:

- ✅ Минимальный SDK: 22 (Android 5.1)
- ✅ Target SDK: 34 (Android 14)
- ✅ Подписанное приложение
- ✅ Иконка приложения
- ✅ Privacy Policy (если собираете данные)

### Проверка подписи:

```bash
# Для AAB
jarsigner -verify -verbose -certs app-release.aab

# Для APK
jarsigner -verify -verbose -certs app-release.apk
```

---

## ⚠️ Важные замечания

1. **Keystore**: Храните keystore и пароли в безопасном месте. Без них невозможно обновлять приложение!
2. **Версия**: При каждом обновлении увеличивайте `versionCode` в `build.gradle`
3. **Тестирование**: Протестируйте release версию перед загрузкой
4. **Privacy Policy**: Убедитесь, что у вас есть Privacy Policy, если приложение собирает данные

---

## 🆘 Проблемы?

- **Ошибка подписи**: Проверьте, что `keystore.properties` существует и содержит правильные данные
- **Ошибка сборки**: Убедитесь, что веб-версия собрана (`npm run build`)
- **Иконка не отображается**: Проверьте, что все размеры иконок созданы

